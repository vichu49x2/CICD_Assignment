name: Test

on:
  workflow_run:
      workflows: ['Train']
      types: [completed]
      branches:
          - 'main'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.vichu49x2 }}
        password: ${{ secrets.dckr_pat_bW_ru8VNuSfuYPeiHuxZyO5Mabs }}
    - name: Get the Docker image
      run:
        image tag test:v1 vichu49x2/test:v1
        docker pull vichu49x2/test:v1
    - name: Test model
      run: |
        OUTPUT=$(python test.py)
        echo "Score:"
        echo "$OUTPUT"
        if [[ `echo "$OUTPUT 0.35" | awk '{print ($1 < $2)}'` == 1 ]]; then echo "Insufficient Accuracy" && exit 1; fi
    
